import React, {Component}               from 'react';
import {connect}                        from 'react-redux';

class AnswerPage extends Component {
    render() {
        return (
            <div>
                AnswerPage
            </div>
        )
    }
}

export default connect()(AnswerPage);
